# 实验室助手 · 产品文档（PRD）

> 版本 0.1.0 ｜ 2026-09-06 ｜ 目标用户：化工药学大类大学生

## 一、产品定位

**实验室助手**是一款桌面知识库问答应用：把你自己的课程资料（实验讲义、药典通则、SOP、课件等）导入后，用自然语言提问，AI 依据资料回答并**标注引用原文出处**，点击即可逐字核对——杜绝"一本正经胡说八道"。

- 一句话：**「能查原文的课程资料问答助手」**
- 平台：Windows 10+ / macOS（未签名需右键打开一次）
- 形态：本地桌面 APP，无服务器、无云存储，数据全部本机保存

## 二、核心功能

### 1. 知识库问答（内置库 + 自建库）
- **内置「QC检验知识库」**：开箱即用，含《中国药典》2025 年版通则整理的 10 大类演示语料（实验室基础操作 / 试剂与溶液配制 / 一般鉴别 / 一般杂质检查 / 含量测定 / 溶出度 / 微生物与无菌 / 仪器分析 / GMP / 标准与参考）。
- **自建知识库**：按课程创建（如"仪器分析""有机化学实验"），支持导入 txt / pdf / docx（单文件 ≤20MB、≤200 页）。
- 提问时可选单个知识库或"全部"；切换知识库自动开新对话。

### 2. 引用原文对照（防幻觉核心）
- 每个结论后紧跟引用编号 [1][2]，徽章可点击；
- 回答下方附「引用来源」列表：原文段落 + 文件名 + 相似度分数，可展开逐字核对；
- 检索零命中时直接回答"资料中未找到相关内容"，**不调用大模型编造**。

### 3. 多轮追问
- 同一对话内连续追问（"什么是重金属检查法？→它分几种方法？→第三法适用什么条件？"），结合上文理解指代。

### 4. 笔记导出（用户拍板加入）
- 一键把当前对话（问题+回答+引用原文展开）导出为 **Markdown（.md）** 或 **Word（.docx）**，方便写实验报告、复习笔记；默认保存到"下载"文件夹。

### 5. 召回调试（维护者工具）
- 设置页 →「召回调试」：输入问题查看检索命中的原文与相似度分数，可临时调 top_k 与阈值——排查"资料里有却答不出"的唯一入口。

### 6. 设置
- 替换 API Key（内置 Key 开箱即用，可换成自己的）；调整检索参数（top_k、相似度阈值）；查看费用说明与数据目录。

## 三、非目标（明确不做）

| 项目 | 说明 |
|---|---|
| 图片 OCR 入库 | 二期（中文手写/公式识别效果有限） |
| 多人协作 / 云同步 | 单机应用 |
| 微信小程序 / 网页版 | 需服务器+备案，用户已否决 |
| 离线问答 | 问答需联网调用大模型 API；文档与历史本机保存 |

## 四、界面与操作流程

- **界面**：左侧深色侧边栏（知识库列表 + 新建 + 设置入口），主区聊天视图（顶部知识库切换/导出/新对话，中部消息流，底部输入框），右侧引用来源抽屉，设置页含「基本设置 / 召回调试」两个标签页。
- **典型流程**：
  1. 首次启动：向导页（可选填自己的百炼 Key，或直接跳过用内置 Key）；
  2. 侧边栏「＋」新建知识库：命名 → 选择文件或整个文件夹 → 自动复制并建立索引（进度条）；
  3. 聊天区提问 → 流式回答 → 点击 [n] 徽章核对原文；
  4. 「导出笔记」→ 选 Markdown 或 Word → 到"下载"文件夹取文件。

## 五、数据与隐私

- **全部本地存储**：文档副本、向量索引、对话历史、设置，均在用户目录（Windows：`%APPDATA%\LabAssistant\`）。
- **唯一联网行为**：问答时把检索到的文档片段发送给阿里云百炼（大模型服务）用于生成回答；请勿导入涉密资料。
- 删除知识库即删除其文档副本与索引；对话历史保存在 chat.db。

## 六、费用说明

- 按量计费：每次问答约 **1~2 分钱**；充值 20 元可长期使用。
- 余额查询与充值：百炼控制台 https://bailian.console.aliyun.com/
- 余额耗尽会报错，充值后即可恢复。

## 七、常见问题

| 现象 | 处理 |
|---|---|
| 双击 exe 被杀毒软件拦截 | 本项目未签名，属误报；点"更多信息→仍要运行"，或将程序目录加入白名单 |
| 启动报错缺少 WebView2 | 安装微软官方 WebView2 运行库（app 会弹窗给下载链接） |
| 回答"资料中未找到"但资料里有 | 换问法或去「召回调试」看命中分数；可调低阈值 |
| 回答细节与原文不符 | 大模型偶有发挥；正式场合以引用的原文为准（QC 合规要求人工复核） |
| 断网时提问报错 | 问答需联网；已导出的笔记与历史对话不受影响 |
| 扫描版 PDF 导入失败 | 文字层缺失，OCR 属二期功能 |

## 八、二期展望（按需，未经用户确认不实施）

1. 图片 OCR 入库（手写笔记/课本拍照）；
2. 语料扩充（药典网络版更多通则、实验安全规范）；
3. Mac 版签名（$99/年 Apple 开发者账号，免右键打开）；
4. Windows 安装器与自动更新；
5. 会话历史列表（切换/删除历史对话）。
