# 不存档的内容（都可重新生成，或含密钥）：

# Python 虚拟环境（几个 G，可重建）
venv/

# 打包产物（可重新打包）
dist/
build/work/

# 本地密钥文件（API Key，绝不进存档/云端）
local_key.txt

# Python 缓存
__pycache__/
*.pyc

# 公开版：内置库语料目录不入库（仅保留 README 占位）
res/builtin_kb/docs/*
!res/builtin_kb/docs/README.md

# 评测报告（运行产物，本机留存）
tests/eval_reports/
